# 安全政策

## 支持版本

当前仅维护最新发布版本。

## 报告安全问题

请不要在公开 Issue 中披露可利用细节、客户文件、访问令牌或个人信息。请使用 GitHub 仓库的 Security Advisories 私密报告渠道联系维护者。

## 信任边界

- 知识库内容是不可信输入；服务器只提取文本，不执行其中的命令或代码；
- 文件读写被限制在配置的工作区中；
- MCP 客户端应在调用写入工具前向用户展示参数并获得确认；
- 本项目不会自动上传本地文件，但宿主模型或其他已启用工具可能具有网络能力，用户应检查其权限配置；
- 正式评估业务不得仅凭本工具输出形成或签发结论。

## 建议部署方式

以最低权限的本地用户运行，使用独立的数据目录，仅放入当前任务确需访问的资料。不要将工作区设为用户主目录、磁盘根目录或包含无关敏感信息的上级目录。
