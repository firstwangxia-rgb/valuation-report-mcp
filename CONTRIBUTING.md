# 贡献指南

感谢参与 `valuation-report-mcp`。

## 开发流程

1. Fork 仓库并创建主题分支；
2. 安装开发依赖：`python -m pip install -e ".[dev]"`；
3. 为行为变化补充测试；
4. 运行 `ruff check .` 和 `pytest`；
5. 提交范围单一、说明清楚的 Pull Request。

## 数据与版权

- 不得提交客户资料、未公开财务数据、含个人信息的文件或受保密义务约束的材料；
- 不得直接提交无再分发许可的准则全文、教材或历史评估报告；
- 测试资料应为原创、匿名化数据或具有明确开放许可的数据；
- 引用公开资料时保留来源 URL、标题和获取日期。

## 代码约定

- 支持 Python 3.10+；
- 对外工具使用 Pydantic 模型定义边界；
- 所有文件访问必须经过工作区路径校验；
- 新增写入工具时，明确 MCP 工具注解并增加路径穿越测试；
- 不在服务端内置搜索密钥、账号令牌或未经说明的遥测。
